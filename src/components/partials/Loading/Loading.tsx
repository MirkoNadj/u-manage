import React, { FC } from "react";
import './Loading.css';

export const Loading: FC = () => {
    return (
        <div className="loading">
            <h1>LOADING POSTS...</h1>
        </div>
    )
}